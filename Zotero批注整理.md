# Zotero 批注整理

完整执行顺序、模型交接和批次验收见 [日常整理 SOP](日常整理SOP.md)。本页说明采集与片段字段的技术约束。

阅读时继续按自己顺手的方式标注；无需换颜色、手工复制原文或填写分类。整理时以论证联系决定素材边界。

## 只读采集

1. 搜索具体书目。独立 PDF 附件可能被默认搜索排除；可从最近批注的 parentItem 找到附件。不能因搜索无结果而断言库中没有该书。
2. 对已知附件调用 get_annotations，使用 JSON 格式和原生批注读取，不无范围扫描全库。
3. 保存规范化返回记录到 `inbox/zotero/`。准备组合某几条时，再读取这些批注的 JSON 元数据，保留 sort index 与 position；不要按创建时间排序。
4. 原始快照只新增、不覆盖；后续批注发生变化时保存新快照，由执行者比较批注 ID、内容、备注和位置，判断如何局部修订已有素材，不能覆盖用户已修改的卡。按 SOP 留存逐条去向；目前没有自动增量同步脚本。

## 组合与解释

先按附件、页及页内位置阅读，再判断语义联系。页面相邻只是线索，不是自动合并的依据。颜色也不是分组键。

组合结果写入 `materials/MAT-….json`，使用 schema 的可选 `capture.segments`、`raw_snapshot` 与 `grouping_reason`。保留每条文字、颜色、comment 和位置；用户已说明重叠来自重复划线：阅读正文去掉同一处重叠，原始快照保留；缺字和可疑识别结果明确记录。不把未划出的文字猜出来；必要时读取或查看相应 PDF 页核对。

需要深入解读时，在 `analysis.argument_structure` 内解释同一论证的观点、理由、例子及其作用，并引用本卡的 annotation_key。不是每篇都要有完整的四段结构；缺证据时明确留空或列问题。`argument_uses` 可在实际写作调用时说明具体用途与边界，默认留空，不预先匹配题目。

目前由 Codex 阅读后选择分组并写入结构化卡片，没有按颜色或距离自动分组的脚本。本次 70 条批注已全部整理为 8 张卡，详见 [本次整理总览](试运行/刘擎本次整理.md)。

## 校验与阅读

在 EssayLab 目录执行：

```sh
python3 scripts/materials.py validate
python3 scripts/materials.py render
```

检查会拒绝重复批注 ID、跨附件组合、错误排序、无依据的片段改写与悬空的论证引用。生成页面显示去重、衔接后的摘录，保留段间空隙；来源 ID 和去重记录折叠展示，非空 comment 始终显示。

原文核对、事实核验、读者认可仍各自记录。本轮已回读 PDF 上下文，但尚未逐字视觉核验全部高亮，具体卡片也未获读者内容认可。

## 去重与补充上下文

`segments.text` 留存采集值。`display_text` 仅在重复划线时使用：必须是原片段的连续子串，允许空串代表整条被覆盖；必须填写 `cleanup_note`，删去的文字须能在相邻来源中找到。`join_previous` 仅用于已按原文核对的连续断句，不能越过未划内容。`text_original` 由这些展示片段拼接产生，阅读排版只去掉汉字间的行内空格，保留段落间隙。

相同观点在不同段落承担不同作用，不因主题相似而删去。重复选区的 comment 也不能删除。

为理解论证而补读的正文存为 `context_excerpts`（id、page_index、text、method），与高亮分区展示。论证步骤可以通过 `context_keys` 引用它们；每步须有实际高亮或补充正文依据。正文自身坏字应注明，不能用模型猜测冒充校正。
