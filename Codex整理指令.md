# 阅读素材整理指令 v0.3（简单素材库）

执行入口见 [日常整理 SOP](日常整理SOP.md)，包含模型分工、步骤、示例和验收；本文件保留内容规则。依据 `素材数据规范.md` 与 `templates/material.schema.json` 整理真实摘录，按用户实际阅读批次处理，不要求先达到某个数量。仅在需要分类时阅读 `taxonomy/themes.json` 的定义和 boundary，日常不强制分类。

## Zotero 优先入口与素材边界

已连接 Zotero 时直接只读获取指定附件的批注，不要求用户复制原文。按 [Zotero批注整理.md](Zotero批注整理.md) 保存原始快照并组合素材。手工摘录仅为备用。

一条批注不等于一条素材。先按页内位置读取，再判断哪些片段共同回答一个问题；不要按颜色分桶或将观点、理由、例子各建一张卡。颜色只保留为线索，不自动决定内容类型。没有必要时不强迫所有片段合为整章。

用户已明确重复划线来自笔不灵敏，整理正文应去重；按 `display_text`/`join_previous` 规则衔接，原始快照仍留存，不因去重丢掉 comment。不得删除不同位置为推进论证而再次出现的相似观点。

组合卡保留 `capture.segments`，每条的原文、comment、颜色和位置独立留存；`grouping_reason` 解释组合理由。需要深入解读时，`analysis.argument_structure` 可在同卡说明观点、推理、例子/反例的作用，并引用实际批注 ID；补读的正文放在 `context_excerpts`，用 `context_keys` 引用，不能假装用户高亮里已有这个步骤。它是对作者论证的解释，`argument_uses` 才是面向写作的新用法，两者不能混同。缺失步骤列为待核对，不用常识补成作者原话。

## 手工录入（备用）

用户可以只给原文，来源和位置不足则保留待补。将用户实际提供的内容转换为 `manual-batch.schema.json` 的批次输入，使用 `scripts/materials.py import` 创建卡片。不要把占位符、题目材料或模型编写的例句当成 20 条真实阅读样本。

导入不等于分类。确认文字已存后，逐条阅读并写入 `materials/MAT-….json`。JSON 是唯一编辑版本，Markdown 为生成的阅读副本。不可仅修改后者。

## 整理

保留原始摘录、颜色和读者留言。`context_before / context_after` 只放原文上下文；你的说明写 `context_note`。不知道作者、页码或完整语境就说不知道，不补造。

默认填写标题 `title`、一句话概括 `summary`、去重后的原文与出处，以及用户实际提供的备注。`summary` 属于 AI 概括，不是原文或读者想法。主题、概念、论证结构和作文用法均可为空，不要求用户填写，也不为凑字段生成解释。只有用户需要深入整理时，再说明具体论点、推理连接与边界。

反例必须对应它反驳的断言；反驳推理可用 counterargument，不假装是实际反例。解释性观点不冒充事实证明。没有出处证据的哲学概念只列候选，不把它写成作者使用的理论。没必要时概念可为空。

默认不预先匹配题目；用户实际写题时再检索材料、说明适配理由与局限。若明确要求保存关联，题号必须存在且理由具体，最多三道；已有关系可保留为可选资料，不成为日常录入要求。

## 状态与修订

完成整理设为 `draft`。`source_match` 记录是否对照来源核对文字；各论证用法的 `evidence_status` 记录事实状态；用户是否认可由 `status` 与 `reviewed_by / reviewed_at` 记录。这三者不得互相代替。

只有用户实际表达内容认可，才记录 `reviewed`，不可补造确认人或确认日期。不把用户赞同整体流程解释为他已认可未来所有素材。人工确认不要求作者观点一定正确，反对的材料同样可以入库。

修改原文前应有核对依据，在 review_notes 说明修正并保留原始录入。已确认素材的实质性解释修订先回到 draft，并保留原确认背景与修订说明，不沿用过时的已认可标记。

每批完成后执行字段与交叉引用校验，再生成阅读卡。报告实际处理数量、待补出处和可讨论的分类边界。测试样本不进入个人资料，也不计入真实试运行。

## 按需调用

日常任务是整理题目、整理阅读素材。无需启动“初答—读卡—再答”的实验，也不强制关联题目、建立关系图或写作工作台。用户真要写某题时，再帮助查找素材，说明能用在哪里、不能推出什么。是否保存这次调用，依据当次需求。
