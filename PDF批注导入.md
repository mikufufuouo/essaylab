# 从带批注 PDF 到 EssayLab

当前优先使用 [Zotero MCP 只读组合流程](Zotero批注整理.md)。本页 PDF 导出脚本保留为备用，逐高亮产出的卡仅是采集记录，不应直接当作完整论证素材。

本次已用用户提供的《刘擎西方现代思想讲义》实际验证：284 页 PDF 中有 4 条原生 Highlight，黄色、蓝色、绿色、红色各 1 条，均在 PDF 第 9 页。已保存原始批注，生成素材草稿，没有使用 OCR，也没有改写 PDF。

## 本次支持的入口

**Zotero 导出包含批注的 PDF → 放进工作区 → 读取 PDF 内嵌批注 → 独立素材 JSON → Codex 整理 → Markdown 阅读卡。**

这次读的是 PDF 内嵌批注，不是 Zotero 数据库、Markdown 导出、Web API 或账号同步。Zotero 的内置批注默认存于数据库，需要导出时嵌入 PDF，直接复制原附件不一定带批注。官方说明：[批注存储与导出](https://www.zotero.org/support/kb/annotations_in_database)。

你可以直接把导出的文件交给 Codex，不用自己执行命令。本机已为 PDF 适配器建立独立 Python 环境。

```sh
.venv/bin/python scripts/pdf_annotations.py "../刘擎西方现代思想讲义 (刘擎) (Z-Library).pdf" --title "刘擎西方现代思想讲义" --author "刘擎"
.venv/bin/python scripts/materials.py render
```

在新机器上，先建立 Python 环境并安装 `requirements.txt`。上述命令从 EssayLab 目录执行。导入器创建的是未整理卡；语义分析由 Codex 根据真实文字另行完成。不要把导入成功当作已经完成 AI 分类或人工认可。

## 提取时保留什么

- 原 PDF 的 SHA-256 与相对路径；原文件保留在工作区，移动资料库时需一起带走，或修正路径并验证哈希。
- PDF 页索引从 0 开始，本次为 8；人看到的 PDF 页序为第 9 页。印刷页码与 PDF 页标签没有就留空。
- 原始批注 ID、xref、批注字典、颜色数值、透明度、逐行 QuadPoints、批注内容与日期。
- 实际选中的字符及其位置。跨行逐区提取，不能用整段外包矩形替代选区。
- 同页前后各最多 180 字的上下文，单独存储；不自动补进高亮原文。窗口可能截断，不能冒充完整段落。

原始记录存入 `inbox/captures/CAP-….json`，导入审计存入 `inbox/pdf-imports/`。素材通过 `parent_capture_id` 回链。批注署名不是书籍作者，批注 content 也可能是复制文字而非留言；本版保留原值，不自动替读者写留言。本样本四条的 content 均为空。

## 四色映射与未知情况

实际读取的颜色是 `#ffd400 / #2ea8e5 / #5fb236 / #ff6666`，曾在四色测试中对应观点、案例、概念、表达。2026-09-09 起新导入只识别颜色，类型保持 unknown，按论证关系判断用途。规则存于 `config/pdf_colors.json`。历史卡的类型不回写。

仅做 RGB 数值到八位色值的舍入与精确映射；没有视觉色块识别，也没有模糊猜颜色。未知颜色记录为 unknown；无文字层、无有效 QuadPoints、空选区及非 Highlight 批注都保留在审计，不生成虚假的文字。PyMuPDF 对 Highlight 使用 stroke 颜色，接口说明见 [Annot 文档](https://pymupdf.readthedocs.io/en/latest/annot.html)。

字符是否被选中，以字符中心落入高亮四边形判断。本次水平中文选区已对照原页核验；复杂竖排、旋转、多栏、重叠文字层与扫描件仍需另行验证，适配器不承诺所有 PDF 都同样准确。

## 重复导入与修订

同一文件及同一批注可重复导入，不覆盖已整理或人工修改的素材。指纹包含文件哈希与批注身份、位置、颜色、内容。**重新导出 PDF 改变文件哈希后，会视为另一个文件版本；尚未实现跨版本合并或 Zotero 增量同步。**

全批先校验再写入，新文件排他创建，普通失败撤回本次新写入。本版不提供并发写入与断电恢复。原 PDF 从不写入。

## 这次验证的实际边界

4 条是用户真实划出的技术测试样本，不等于 4 条成熟写作素材。黄色暂不归类；另外三条的解读需要相邻原文。蓝色尤其需要前文“不再抱有”的否定关系。原选区已与 PDF 匹配，但书中理论、历史概述及延伸论证没有因此自动得到事实核验。

查看 [本次单文件报告](试运行/刘擎四色测试/四色测试报告.html)。它是按需生成的本次快照，正式数据仍在独立 JSON 中。
