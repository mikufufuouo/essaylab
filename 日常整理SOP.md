# EssayLab 日常整理 SOP v1.2

## 完整概念以素材群呈现（2026-09-10 用户反馈）

如“现代的铁笼”的定义、两种弊端与双重作用，是一个完整概念的不同部分。一级素材库应显示一个素材群入口，二级总览按理解顺序展示分素材，再进入原文详情。不要把这些部分在一级平铺，也不必为了完整性拼成一篇过长摘录。

先判断是一段连续论证（同卡合并）、一个概念的多个侧面（建素材群）、还是独立内容（单独保留）。同属一本书或含相同关键词不是归组依据。日常不要求用户手工维护；执行者整理后检查是否补入既有群或创建新群，成员可以按论证顺序排列，必要上下文仍须保留在各卡内。

素材群定义的唯一编辑版本是 `materials/groups.json`，记录 id、title、summary 和有序 material_ids。一个分素材当前只归一个群；不复制原文、不改原卡 ID。执行 `materials.py render` 或 `build_library.py` 刷新索引，再导出 HTML。校验拒绝不存在的成员、重复归组和测试卡入群。素材数据下载包含素材群，可一起重新导入。

验收须检查：一级不重复平铺群内素材；搜到组内原文或补充上下文能找到所属群；二级显示完整概念说明与全部部分；从详情返回群、从群返回库后，搜索词、焦点和浏览位置保留。现有实现采用页内二级视图，不创建浏览器历史条目。

适用：Zotero 阅读批注整理、用户提供的作文题入库，以及更新可离线阅读的 HTML。2026-09-10 制定。目标是维护简单的题目库和素材库，不增加日常分类负担。

这是可供模型执行的流程，不是已经实现的自动流水线。现有脚本负责数据校验和页面生成；增量比较、批注覆盖检查、语义分组和内容复核仍须执行者实际完成。模型能力分工依据用户当前使用经验，尚未经过本项目对比测试。

2026-09-10 执行复核补充：首批结果出现缺上下文、拆分不当和连续词句断段，见[实卡复核](试运行/2026-09-10-SOP执行质量复核.md)。以下“独立回读验收”是发布前必做步骤。少字段不等于少做理解工作；没有执行日志和对比试跑，暂不能据此认定模型能力差异是原因。

## 独立回读验收（v1.1 新增，适用于下文全部步骤）

**先读源段，再定边界，最后填卡。** 对本批高亮覆盖的段落及其解释依赖，先回读完整相关正文，写出候选分组：本组说明什么、理由/例子/限定在哪、为什么与相邻组分开、要补哪段原文。保存在批次工作记录即可，不增加素材库必填字段。没有必要不扩展到整章或整本书。

正式入库前，从读者只打开这一张卡的角度逐卡检查：

1. **指代闭合**：“这两种观点”“这种能力”“当然，不光是……”等能否在卡内找到对象？不能就补读、保存最短必要上下文，或与承接卡合并。
2. **理由闭合**：读者能否看见判断为什么成立，而非只有结论和比喻？有原文机制就保留机制，不用 AI 常识补充。无论是否高亮，只要决定理解就按补充上下文规则保存。
3. **概括可回查**：概括的每个实质判断必须在本卡摘录或补充上下文找到依据。“我在原书看到了”但没存入卡内，不算完成。
4. **边界合理**：一张只定义、下一张承接解释同一个问题时优先考虑合并；能各自完整说明不同问题时保留独立。不以卡数少为目标，不把全部相邻内容并成长章。
5. **段落可读**：排查单字、半词、孤立标点和未闭合引号；对照源页确认后设置 join_previous。页内排序不是语句连续性的充分证据，不能全量无差别连接。
6. **排除项合理**：逐条复查“仅导航”。名言、墓志铭、实质备注不是章节号；缺上下文应补读或待核，不能以此理由排除阅读素材。

上下文数量没有最低配额，已有摘录完整就无需补充；但明确缺口不得空着。必要的转折、限定和说话者信息同样属于上下文，不只补好懂的例子。补充内容与高亮分区显示，原始批注不改。

首次交接、换模型或本批质量检查失败后，必须检查全批次分组表及逐卡回读条件，不能只看执行者主动标疑的部分。有已授权的复核模型时交其独立检查（可用 Sol）；没有时执行者做独立第二遍并如实记录未经过独立模型复核。分组未通过前，Luna 不批量写正式卡。后续降低复核频率需以真实试跑结果为依据。

## 1. 选择执行模型

| 模型 | 默认任务 | 遇到什么情况交接 |
| --- | --- | --- |
| GPT Luna | 已确定分组的摘录整理、明确重复的清理、来源录入、运行现有脚本；通过试跑后可独立整理简单批次 | 分组不明确、长距离引用、反讽或多方观点、缺字影响含义时交 Terra |
| GPT Terra | 日常默认执行者：采集、分组、去重、简短概括、入库和检查 | 补读上下文后仍有两种会改变含义的解释，或复核持续发现语义错误时交 Sol |
| GPT Sol | 局部疑难复核、首次试跑抽检、必要的规则修订 | 缺少可靠原文就保留待核，不以模型能力替代来源证据 |

用户只说“继续整理”时，沿用当前执行模型和本 SOP；这张表不授予自动启动子代理或另一个模型的权限。有可用且已授权的模型调用方式才实际交接，否则留下复核材料，继续完成清楚的部分。不要声称调用了未调用的模型。

不要求每一批都经过三个模型。不让 Sol 为了复核自动增加主题、论证解读或题目匹配。

## 2. 开始前读什么

1. 本文件：工作顺序与验收。
2. [整理规则](Codex整理指令.md)：内容原则。
3. [Zotero 技术说明](Zotero批注整理.md)与[数据规范](素材数据规范.md)。
4. 实际写卡前读 `templates/material.schema.json`、相关现有卡和 `scripts/materials.py`；不要凭记忆构造字段。

用户当次明确要求优先。旧卡可能有深度分析、主题和题目关联，只参考其字段格式，不照抄复杂程度。当前默认新卡只有标题、摘录、一句话概括、出处和真实备注。

## 3. 确定批次与保存原始记录

1. 根据对话与已有快照确定书目、附件和上次已处理范围。存在多个无法区分的候选才问用户；不无范围扫描全库。
2. 通过 Zotero MCP 的只读能力获取原生批注 JSON；按实际可用工具签名调用。组合用到的记录还须获取位置、页内排序等原始元数据。不要要求用户复制原文，不写回 Zotero，不为本任务安装插件或开启索引。
3. 将返回数据和必要元数据保存到 `inbox/zotero/` 的新快照。文件名包括附件 ID 与实际采集日期，同日重跑加时间或序号；禁止覆盖历史快照。
4. 比较批注 ID 与历史快照、已有素材的 `capture.segments`。分别列出新增、内容或备注或位置变化、未变化记录。不能只按“今天创建”筛选；读者可能补改旧批注。未出现在本次结果中的旧批注只记差异，不据此删卡。
5. 新增但已入库的 ID 不重复建卡。旧批注变化时，保留原快照和旧卡的用户修改，记录差异，判断受影响卡是否需要局部修订；不以整卡覆盖方式同步。

读取失败时报告实际错误，保留已取得数据。没有读到新增批注时，不编造新卡或宣布完成今日整理。

## 4. 建立批注去向表

在 `试运行/` 新建带日期和附件 ID 的批次记录，先记处理范围，结束时补齐。它是工作记录，不向日常阅读界面添加字段。

对每条待处理批注记录：`annotation_key`、变更类型、去向、素材 ID（若有）、原因。去向采用以下四类：

- **已入素材**：包括正文被重复覆盖、但原始片段和备注仍保存在卡内的批注。
- **已存在**：指出已有卡 ID，避免再次建立同一素材。
- **仅导航**：纯章节号、孤立标题等，说明为何不单独成为素材；原始快照仍保留。带实质备注或与正文相连的标点不得随意归入此类。
- **待核**：缺上下文、缺可靠文字、分组未决等，保留具体阻碍与下一步。

每条恰有一个主要去向。同一批注若确需被多卡引用，列出所有卡 ID 和理由；默认不重复收录。同一处重叠不等于可从去向表中消失。

结束时核对：待处理 ID 集合 = 四类去向 ID 的并集，且分类之间不重叠；每个卡 ID 和批注引用实际存在。记录新建、修订、未变卡数，以及四类批注数。批注数与素材数分别统计，不把技术测试卡算作阅读素材。

## 5. 分组、去重与保留上下文

### 分组判断

先按附件、`page_index` 和 `sort_index` 排序，不能按创建时间或颜色排序。

逐组问自己：这些片段是否在讲同一件事？后面的理由、例子、转折是不是在解释前面的判断？拆开后会不会丢掉指代或限制？如果是，放在同卡。出现新的独立问题再拆卡。相邻不自动合并，同主题也不自动跨章拼成一张。

先按上文独立回读验收读取相关完整源段，再确定哪些正文须作为补充上下文保存。分组不要求完整重建作者整章理论，也不为了凑数量拆卡。组内跨度大时检查中间是否发生论题或说话者切换。

### 去重与拼接

- `segments.text`、`comment`、颜色和位置逐条保留采集值。
- 只有确属同一处重复选区，才通过 `display_text` 去掉重叠；它须是该片段的连续子串，可为空，并写明 `cleanup_note` 与覆盖来源 ID。
- 不删除不同位置的相似观点，也不把模型改写放进 `display_text`。
- `join_previous: true` 只用于对照原文确认连续的断句；跨页也须确认连续。中间漏划了内容就保留分段，不能拼成仿佛连续的引文。
- 调用 `scripts/materials.py` 的 `reading_text(segments)` 得到 `capture.text_original`，不要独立手写第二份拼接正文。排版去空格与改动原文字词是两回事。
- 重复片段即使展示文本为空，其非空 comment 仍须保留。

### 缺字与补读

必要的非高亮正文放在 `capture.context_excerpts`，记录真实 PDF 页索引和提取方式；解释写 `context_note`，不混进高亮原文。只补足理解所需的范围。

PDF 文字提取异常时查看对应页；如原页也不清楚，原样保留并说明。不得用熟悉的哲学知识猜补成引文。当前 `display_text` 不是文字校正字段：即使查到正确文字，也保留原始片段，在上下文或说明中记录校正依据，避免绕过现有校验。缺字妨碍核心含义时保留待核，不生成确定性概括。

## 6. 写成简单素材卡

写入 `materials/MAT-….json`，ID 唯一且稳定，新增前检查现有 ID。旧卡修订沿用原 ID。

| 内容 | 写法 |
| --- | --- |
| `title` | 直接说清本段讨论什么，不用“万能素材”“高分必备”或原文不支持的宏大判断 |
| `summary` | 一句话 AI 概括，保留关键条件、转折、否定与观点归属；宁可稍长，不删掉限定 |
| `capture` | 去重摘录、原始 segments、快照路径、简短 grouping_reason、必要上下文和来源 |
| 来源 | 作者、书名、可回找的位置；不把 PDF 第几页当作印刷页码，不猜版本或原著出处 |
| 备注 | 原批注 comment 留在片段；只有用户针对整卡的真实留言才放 `reader_note`，没有就为空 |
| 状态 | `draft`、`collection_role: reading`；未获逐卡内容认可时，确认人和日期保持空 |

`analysis` 的 schema 必填项用合法空值保留：`paraphrase: null`、`primary_theme: null`、`secondary_themes: []`、`classification_reason: null`、`concept_candidates: []`、`argument_uses: []`、`related_prompts: []`、`open_questions: []`。有真实待核问题才填 open_questions。可选的 question 与 argument_structure 默认省略。

不得把“刘擎转述韦伯”写成自己核过的韦伯原著引文。读取成功不代表原文逐字核对，`source_match` 默认 unchecked；真正核对后才按规范记录依据。内容一致、史实可靠和读者认可分别记录。

## 7. 作文题入库（本批有题时执行）

1. 逐字保存用户给出的题面，不补作文字数、文体限制、考试年份或地区。
2. 在 `taxonomy/prompts.json` 查完全重复和近似题。完全重复保留已有 ID 并记录新来源；近似题不自动合并。
3. 用户自己想到的题标为 `personal_question`（自拟思考），记录提供日期与“用户自拟”的来源。日期不冒充考试年份；即使用户准备上海作文，也不冒充上海模考题。
4. 使用现有题库字段和稳定的新 ID；来源原始记录保存在 `sources/`。主题若当前界面确有要求，做最少的归类并标 AI 草稿，不添加题目—素材关系。
5. 更新前检查构建与校验脚本的来源支持。当前 `validate_library.py` 对 SRC-001、SRC-002 及原有 121 题有专门断言，`test_viewer.cjs` 也有固定数量断言；首次增加个人来源必须窄范围扩展支持，保留历史来源核验。不要删除断言以求通过，也不要把新题伪装为 SRC-002。
6. `import_prompt_supplement.py` 是既有补充题包的专用脚本，不能当通用题目导入器重跑。执行者无法安全扩展新来源时，先保存题面和待入库记录，明确报告尚未入库。

例：用户提供“生活中，人们常常追求‘更多’；如今，也有越来越多的人开始追求‘更少’。对此，你怎么看？”时，标题可拟“更多与更少”，题面仍以用户实际提供的字符为准，来源为自拟思考。本例只是规则演示，不表示这道题已入库。

## 8. 内容自检与局部升级

每张新建或实质修订的卡，执行者必须对照原文检查：

- 是否遗漏“可能、只有、并非、但”等会改变结论的词？
- 是否把作者批评的观点当作作者主张，或混淆转述者与原作者？
- 例子是否仍连着它解释的判断？是否把说明性例子当成普遍证明？
- 概括是否新增原文没有的因果、价值判断或普遍结论？
- 去重、拼接是否真有位置和文字依据？备注是否完整保留？

出现以下情况，先补读必要上下文；仍未解决才升级：说话者不明；两种分组导致不同理解；核心缺字；概括需要新增前提才能成立；反讽或多层转述无法确定；已有认可卡与新批注存在实质冲突。

不要输出虚构的置信分数。复核包包括：卡 ID、批注 ID、页码、原文与必要上下文、当前概括、具体疑问、候选解释及差别。复核者只改有依据的部分；无可靠文字则维持待核。

## 9. 生成与校验

JSON 是唯一编辑版本；Markdown、索引、HTML 都由它生成。在 `EssayLab/` 目录依次执行，任一步失败先排查，不宣称整批完成：

```sh
.venv/bin/python scripts/materials.py validate
.venv/bin/python scripts/materials.py render
.venv/bin/python scripts/build_library.py
.venv/bin/python scripts/export_latest_html.py
.venv/bin/python scripts/validate_library.py
```

使用项目已有环境；缺依赖时报告或按已授权的项目依赖方式补齐，不默默更换数据格式。

上述校验能发现字段、片段排序、部分无依据删改及引用问题，不能证明语义正确，也不会自动完成第 4 节的全批次覆盖核对。两类检查都要做。

只变数据和文档时，不要求新增测试。若修改了页面、来源支持或校验代码，运行相关已有测试；页面交互检查的现有命令是：

```sh
NODE_PATH=/tmp/essaylab-dom-check/node_modules node scripts/test_viewer.cjs
```

该命令需要已有 jsdom 环境，具体安装说明见 README。检查页面搜索、新卡摘录与备注、来源页码和技术测试卡默认隐藏。无法实际打开页面时说明只做了数据或 DOM 检查，不冒充视觉验收。

## 10. 首次交接试跑

先用一小批真实批注试跑，建议约 10–20 条，覆盖连续断句、重复划线、转折与备注；不要为凑样本伪造用户批注。测试候选卡先放独立试运行目录，不再次导入正式库。

执行者逐卡自检；首次由 Terra 或 Sol 对照来源复核全部试跑卡。复核目标是忠实性，不要求标题措辞完全相同。通过标准：

- 每条批注去向明确，没有丢失独有文字或 comment。
- 没有无依据的拼接、引文改写或来源伪造。
- 没有会改变原文含义的概括错误，全部待核项如实列出。
- 结构校验通过；相同输入再跑不产生重复正式卡。

通过后 Terra 可日常独立执行；Luna 独立执行范围以试跑通过的任务为限。首次换模型、换来源格式或修改规则后重新抽检。稳定阶段无需每批强模型检查：遇到疑难再局部交接；偶尔抽取含转折或去重的卡检查。如发现改义、丢备注或误删，复核该批所有受同类操作影响的卡并调整规则。

## 11. 用户看到的完成报告

保持简短，报告实际结果，例如：

> 新增 X 张素材，修订 Y 张；重复划线已处理，备注保留。收录 Z 道题，其中自拟题 Z1 道。还有 N 处待核：简述原因。已更新最新版 HTML。检查：列出实际通过的检查。

附最新版 HTML 链接。有待核项或未成功入库的题必须说明，不用“全部完成”掩盖。不要把 SOP 写成之后自动运行的承诺；每次仍需用户发起任务或另行配置自动执行。

## 12. 可直接复制的任务指令

### 日常使用（推荐 Terra）

```text
按 EssayLab/日常整理SOP.md 继续整理本次阅读，并处理我提供的作文题。
只读 Zotero，先比较历史记录，仅处理新增及变化内容。
默认只做简单素材卡，去掉同处重叠、保留上下文和全部备注，不预先关联题目。
逐条记录批注去向，逐卡对照原文自检，更新数据和最新版 HTML，并执行现有校验。
按 v1.1 先回读完整相关源段，再确定分组；正式入库前检查指代、理由、概括依据及段落连续性，复查所有导航排除项。必要上下文必须保存在卡内，不能只记录“已补读”。
无法确定的部分保存为待核并给出局部复核包，继续完成其余内容；不要猜补，也不要擅自启动其他模型。
最后只报告新增/修订数量、题目入库结果、待核事项和 HTML 链接。
```

### Luna 接手已确定的分组

```text
按 EssayLab/日常整理SOP.md，处理本批已给出的分组清单。
先核对每组批注 ID 和出处，保留原始片段、颜色、位置与全部 comment。
只处理有明确依据的重复与连续断句，写简单标题和忠实的一句话概括。
不自行扩展解释或重划疑难分组；遇到疑问记录具体原文、页码和问题，留给 Terra 复核。
执行批注去向核对和数据校验，区分正式卡与试跑候选卡，不重复入库。
```

### Terra / Sol 局部复核

```text
按 EssayLab/日常整理SOP.md 的内容自检标准，只复核附带卡片和疑问。
逐项对照原文、来源位置和必要上下文，判断分组、去重、拼接及概括是否改变含义。
指出具体问题和证据，给出最小修订；有多种合理解释或缺可靠文字时保留待核。
不要增加标签、题目匹配或长篇思想解读，不以措辞偏好判定错误。
复核通过不等于用户认可，不修改 reviewed 状态。
```

## 13. 正反例（以下均为流程教学用虚构片段，不入库）

**同处重叠**：A 为“选择需要权衡，但权衡之前”，B 为“但权衡之前，应先明确何为值得。”，且位置核实为同处连续重叠。保留 A 原文，B 的 display_text 可为“，应先明确何为值得。”并设 join_previous，记录删去前缀由 A 覆盖。读起来是“选择需要权衡，但权衡之前，应先明确何为值得。”。A、B 的 comment 都保留。只凭字面相同、未核位置则不能这样删。

**漏划中间句**：A 为“人们追求更多。”，后面漏划了条件说明，B 为“因此，有些人开始追求更少。”。两段可在同卡，但不得直接无缝拼接；必要的中间原文作为补充上下文单列。

**概括保留边界**：原文为“科学可以帮助我们比较手段，却不能替我们决定什么值得追求。”。可概括为“科学能帮助选择手段，但不能替人决定追求的价值。”；不可写“科学没有价值”或“理性无法帮助人生选择”。

**观点归属**：原文为“有人认为占有越多越自由，作者接着质疑这一判断。”。可概括为“作者质疑把占有量等同于自由的看法。”；不可写“作者认为拥有越多就越自由”。

**例子与判断**：判断为“什么算损失取决于评价标准”，随后用一位主动舍弃财物的人解释。默认放同卡；不可单拆成“舍弃使人高尚”的故事，更不能概括为“拥有越少越幸福”。
