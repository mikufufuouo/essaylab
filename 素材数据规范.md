# Material Schema v0.2

先让人容易留下摘录，再让结构化数据帮助整理。第一批不要求你填写完整卡片，也不要求每段文字产生一堆理论和关联题。

## 录入时：一个必填项，两个尽量补齐的项

| 项目 | 要求 |
| --- | --- |
| 原文 `raw_text` | 唯一必填，逐字保留，包括暂时看不懂的地方 |
| 来源 `source_title` | 尽量给书名或文章标题，缺失可留空 |
| 位置 `location` | 页码、章节、链接内位置或电子书定位，不知道可留空 |
| 其他 | 作者、链接、颜色、读者留言、真实上下文均可选 |

手工输入不需要伪造高亮颜色。未填颜色保持 unknown；给出颜色也不自动推导类型；只有用户明确提供类型才记录，母题交给 Codex 理解后判断。自己粘贴的文字不会自动算作已经核对原书。

你可直接用 [文字模板](templates/手工摘录.md)，Codex 负责转换为 [批次输入格式](templates/manual-batch.schema.json)。缺来源的记录可以先收，保留待补状态。

## 整理后：一张卡分为五部分

| 部分 | 保留什么 |
| --- | --- |
| `capture` | 原文、真实上下文、来源位置、颜色、提取方式 |
| `reader_note` | 你实际留下的留言，没提供就为空 |
| `analysis` | AI 转述、母题、概念候选、论证用法、关联题与待讨论问题 |
| `source_match` | 是否对照来源核对了原文及核对依据 |
| `status` 与确认记录 | 未整理、草稿、读者已认可，以及谁何时确认 |

完整约束见 [Material JSON Schema](templates/material.schema.json)。`schema_version` 为 `0.2.0`；原题库的版本仍为 `0.1.0`，二者独立演进。

`capture.context_before / context_after` 只能放真正的上下文原文。若是 AI 说明“这段位于某题之后”，写入 `context_note`，不伪装成作者原文。旧单摘录卡继续通过 `parent_capture_id` 回链原始摘录；新组合卡以多个来源片段为依据，不能拆掉转折与限制词。

## Zotero 多批注组合（兼容扩展）

优先从 MCP 只读采集，无需手工复制。`schema_version` 仍为 `0.2.0`，以下字段为可选扩展，旧单摘录卡继续有效：

- `capture.segments`：逐条保留 annotation_key、attachment_key、page_label、page_index、sort_index、position、text、comment、color。按页及 sort_index 排序，同一组合只含同一附件的批注，ID 不重复。
- `capture.raw_snapshot`：相对 EssayLab 的原始读取快照路径；`grouping_reason`：AI 说明为何这些批注应在一起。
- 组合卡的 `text_original` 由片段的 `display_text`（未填则用 `text`）拼接；完全重复可为空，连续断句可用 `join_previous` 衔接。去重必须注明 `cleanup_note` 并有相邻原文覆盖，不能凭此补写或删除独有内容。原始 `text` 不变；阅读卡不重复展示重叠文字。
- 组合卡不压成一个颜色或类型，顶层 highlight 保持 unknown；各条实际颜色见 segments。
- `analysis.argument_structure`：同卡说明原文论证关系，每步含 role、summary 与 annotation_keys。所有引用必须属于本卡；也可用 `context_keys` 指向本卡 `capture.context_excerpts` 中补读的 PDF 正文，每步至少有一项来源。补读正文与用户高亮单列；这是 AI 解释，不是读者留言。

原始 comment 逐条保留，`reader_note` 只放用户明确针对整张素材的留言。读取成功不等于对照 PDF 核对文字，默认 source_match 仍为 unchecked。

完整流程及首张样本见 [Zotero批注整理.md](Zotero批注整理.md)。

## 三件事分别记录

1. **原文核对**：`source_match.status` 为 `unchecked / matched / mismatch`。后两者必须写清来源位置、核对依据、核对者和日期。`matched` 只表示摘录与具体来源一致。
2. **事实核验**：每项论证用法使用 `evidence_status`，可为 `interpretive / needs_fact_check / fact_verified`。若标为事实已核验，必须提供 `verification_basis`，指出核对了什么、依据何在。解释性的推理不冒充事实证据。
3. **人工认可**：`status` 为 `inbox / draft / reviewed`。`reviewed` 必须有实际确认人、日期、可回找的出处以及非空概括或转述（不要求分类或关联题目）。认可的是这张卡的转述和用法，不等于赞同原作者或认可事实可靠性。

这比一个 `source_verified: true` 更明确。源头本身错误时，准确摘抄仍可能得到不可靠的事实。

## 深入解读与写作用法（可选）

日常不要求填写论证用法、概念或关联题，空数组即可。用户实际调用素材时，再解释用途与边界。

每个 `argument_uses` 条目同时记录：

- `claim`：具体讨论哪句话。
- `relation`：支持、挑战还是限定。
- `role`：定义、机制、案例、反例、反驳论证、边界或综合。
- `reasoning`：原文怎样连接到这个论点。
- `conditions / limits`：成立条件与不能推出的东西。
- `evidence_status`：这项用法目前凭什么成立。

`counterexample` 是能挑战某个断言的具体反例；`counterargument` 是反驳推理，二者分开。条件和边界可以暂为空，不凑套话。单独罗列“supports”与“argument_roles”容易丢掉对应关系，所以本版保留逐项绑定。

主母题最多一个，次母题最多两个；不够明确可以暂留空。概念如果仅由 AI 联想到，标为候选。只在明确需要保存关联时填写，最多三道，必须有真实题号和理由。没有适配题目也值得保存。

## 唯一编辑版本与修订

`materials/MAT-….json` 是当前唯一编辑版本，同名 Markdown 从它生成。人工可以直接告诉 Codex 如何改，由 Codex 更新 JSON、校验、重新生成阅读卡。不要把重要修改仅保存在会重新生成的 Markdown 中。

原始录入另存在 `inbox/captures`。一般修改只改 `analysis` 或补全来源；若发现摘录抄错，修正卡片并在 `review_notes` 记录原误字与核对依据，保留最初录入。ID 不随内容修正变化。当前没有双向编辑、自动合并或完善的版本历史，后续组织 Obsidian 时再确定。

导入使用整条输入的哈希识别精确重复，包含出处与留言。相同句子来自不同书不能合并，补充了不同留言也保留为新条目，避免丢失读者想法。

## 简单浏览字段

`title` 为素材标题，`summary` 为一句话 AI 概括；二者与 `capture.text_original`、出处和 `reader_note` 组成日常阅读卡。`collection_role` 可区分 reading 和 technical_test，测试默认隐藏。`question` 及 `analysis` 内的解释、主题、用途和关联继续兼容，但不作为日常新增素材必须填满的内容；空值及空数组可保留。网页默认只呈现简单阅读信息。
